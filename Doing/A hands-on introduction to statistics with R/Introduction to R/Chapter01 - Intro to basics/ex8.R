# Declare variables of different types
my_numeric = 42
my_character = "forty-two"
my_logical = FALSE 

# Check which type these variables have:

class(my_numeric)
class(my_character)
class(my_logical)